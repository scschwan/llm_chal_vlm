# 유사이미지 매칭 시스템 - RAG & LLaVA 통합

## 🎯 프로젝트 개요

**CLIP 기반 유사이미지 검색 + RAG & LangChain + LLaVA 모델**을 활용한 제조업 불량 분석 및 자동 대응 매뉴얼 생성 시스템

### 주요 기능
1. **유사이미지 매칭** - CLIP 모델 기반 TOP-K 검색
2. **데이터셋 매핑** - 이미지 데이터셋과 PDF 매뉴얼 연결
3. **불량 분석 (RAG)** - LangChain + LLaVA를 활용한 자동 분석 및 대응 방안 생성 ⭐
4. **통계 대시보드** - 검색/매칭 통계 및 시각화

---

## 📁 파일 구조

```
similar_image_matching/
├── app.py                    # FastAPI 메인 애플리케이션
├── requirements.txt          # Python 패키지 의존성
├── matching.html             # 유사이미지 매칭 화면
├── manual_mapping.html       # 데이터셋 매핑 관리 화면 (수정됨)
├── defect_analysis.html      # 불량 분석 (RAG) 화면 (신규) ⭐
├── dashboard.html            # 통계 대시보드 화면
├── uploads/                  # 업로드된 파일 저장
└── README.md                # 이 문서
```

---

## 🚀 빠른 시작

### 1. 서버 접속
```bash
ssh -i dm-key.pem -p 2022 root@dm-nlb-112319415-f8e0a97d0b99.kr.lb.naverncp.com
```

### 2. 파일 업로드 및 압축 해제
```bash
cd /root
tar -xzf similar_image_matching.tar.gz
cd similar_image_matching
```

### 3. 의존성 설치
```bash
pip3 install -r requirements.txt --break-system-packages
```

### 4. 서버 실행
```bash
python3 app.py
```

### 5. 접속 확인
```
http://dm-alb-112319279-991b4e0889c4.kr.lb.naverncp.com/
```

---

## 🆕 주요 신규 기능

### 불량 분석 화면 (defect_analysis.html)

RAG (Retrieval-Augmented Generation) 기반 자동 불량 분석 시스템

#### 4단계 자동 분석 프로세스

**Step 1: 유사 이미지 검색**
- CLIP 모델로 입력 이미지 임베딩 생성
- FAISS 인덱스에서 TOP-K 유사 이미지 검색
- 유사도 점수 표시

**Step 2: 매뉴얼 매핑 조회**
- 검색된 유사 이미지와 매핑된 PDF 매뉴얼 자동 조회
- 불량 유형 및 문서 메타데이터 표시

**Step 3: RAG 파이프라인**
```
LangChain PDF 로더
    ↓
텍스트 청크 분할 (chunk_size=500, overlap=50)
    ↓
한국어 임베딩 (jhgan/ko-sbert-nli)
    ↓
FAISS 벡터 스토어 구축
    ↓
유사도 기반 문서 검색 (TOP-K=3)
```

**Step 4: LLaVA 멀티모달 분석**
- 이미지 + RAG 컨텍스트 통합
- 불량 유형 판정
- 발생 원인 분석
- 대응 방안 생성
- 예상 효과 제시

#### 화면 구성

1. **진행 단계 표시기**: 현재 처리 단계 시각화
2. **유사 이미지 결과**: TOP-K 검색 결과 카드 형태 표시
3. **매핑된 문서 정보**: PDF 파일명, 불량 유형, 페이지 수
4. **터미널 로그**: RAG & LangChain 처리 과정 실시간 로그
5. **LLaVA 분석 결과**: 
   - 🔍 불량 유형 판정
   - 📋 발생 원인 분석
   - ✅ 권장 조치 방안
   - 🎯 예상 효과

#### 터미널 로그 예시
```
[12:34:56] [INFO] 유사 이미지 검색 시작...
[12:34:56] [INFO] CLIP 모델 로드 중... (openai/clip-vit-large-patch14)
[12:34:57] [SUCCESS] 이미지 임베딩 생성 완료 [768 차원]
[12:34:58] [INFO] FAISS 인덱스 검색 중... (TOP-3)
[12:34:59] [SUCCESS] 유사 이미지 3개 검색 완료
[12:35:00] [INFO] RAG 파이프라인 시작
[12:35:01] [INFO] LangChain 초기화 중...
[12:35:02] [INFO] PDF 문서 로드: defect_manual.pdf
[12:35:03] [SUCCESS] PDF 파싱 완료 (4 pages)
[12:35:04] [INFO] 텍스트 청크 분할 중...
[12:35:05] [SUCCESS] 총 28개 청크 생성
[12:35:06] [INFO] 한국어 임베딩 모델 로드: jhgan/ko-sbert-nli
[12:35:07] [INFO] FAISS 벡터 스토어 구축 중...
[12:35:08] [SUCCESS] 벡터 인덱스 구축 완료 (28 vectors, 768 dims)
[12:35:09] [INFO] 유사도 검색 실행 (TOP-K=3)...
[12:35:10] [SUCCESS] 관련 문서 3개 검색 완료
[12:35:11] [INFO] LLaVA 멀티모달 LLM 분석 시작
[12:35:12] [INFO] 모델 로드: llava-hf/llava-v1.6-mistral-7b-hf
[12:35:14] [INFO] LLaVA 추론 실행 중...
[12:35:16] [SUCCESS] 불량 분석 및 대응 방안 생성 완료
```

---

## 🔄 데이터셋 매핑 화면 개선사항

### 변경 사항

✅ **복수 파일 업로드 지원**
- 단일 이미지 → 여러 이미지 동시 업로드
- ZIP 파일 지원 (데이터셋 일괄 등록)
- 업로드된 파일 목록 표시 및 개별 삭제 가능

✅ **필드 명칭 변경**
| 기존 | 변경 후 |
|------|---------|
| 불량 유형 | 이미지 유형 |
| 불량 설명 | 이미지 설명 |
| (없음) | 데이터셋 이름 |

✅ **불필요한 필드 제거**
- ROI (Region of Interest) 정보 삭제
- 우선순위 삭제

✅ **UI 개선**
- 안내 정보 박스 추가
- 파일 목록 UI 개선
- 데이터셋 중심 인터페이스

---

## 📊 화면별 기능

### 1. 유사이미지 매칭 (`/matching.html`)
- 이미지 업로드 (드래그 앤 드롭)
- TOP-K 유사 이미지 검색 (1-20개)
- 매칭 성공/실패 기록

### 2. 데이터셋 매핑 (`/manual_mapping.html`)
- 복수 이미지 또는 ZIP 파일 업로드
- PDF 매뉴얼 업로드
- 데이터셋 정보 입력 및 매핑
- 저장된 매핑 목록 관리

### 3. 불량 분석 (RAG) (`/defect_analysis.html`) ⭐
- 불량 이미지 업로드
- 자동 4단계 분석 실행
- 실시간 터미널 로그
- AI 기반 대응 방안 생성

### 4. 통계 대시보드 (`/dashboard.html`)
- 검색/매칭 통계
- 차트 및 그래프
- CSV 데이터 내보내기

---

## 🔧 NCP 서버 배포 가이드

### Step 1: 파일 전송
```bash
scp -i dm-key.pem -P 2022 similar_image_matching.tar.gz \
  root@dm-nlb-112319415-f8e0a97d0b99.kr.lb.naverncp.com:/root/
```

### Step 2: 압축 해제
```bash
ssh -i dm-key.pem -p 2022 root@dm-nlb-112319415-f8e0a97d0b99.kr.lb.naverncp.com
cd /root
tar -xzf similar_image_matching.tar.gz
cd similar_image_matching
```

### Step 3: 의존성 설치
```bash
pip3 install -r requirements.txt --break-system-packages
```

### Step 4: systemd 서비스 등록
```bash
sudo tee /etc/systemd/system/similar-image.service > /dev/null <<'EOF'
[Unit]
Description=Similar Image Matching System
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/similar_image_matching
ExecStart=/usr/bin/python3 /root/similar_image_matching/app.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
EOF

sudo systemctl daemon-reload
sudo systemctl enable similar-image.service
sudo systemctl start similar-image.service
```

### Step 5: 상태 확인
```bash
sudo systemctl status similar-image.service
tail -f /root/similar_image_matching/app.log
```

### Step 6: ALB 설정
NCP 콘솔에서:
1. Load Balancer → dm-alb-112319279
2. 타깃 그룹 포트: **8080**
3. 헬스 체크 경로: **/health**

---

## 🌐 접속 정보

### 외부 접속 (ALB)
```
http://dm-alb-112319279-991b4e0889c4.kr.lb.naverncp.com/
```

### 페이지 목록
- 메인: `/` 또는 `/matching.html`
- 데이터셋 매핑: `/manual_mapping.html`
- **불량 분석 (RAG)**: `/defect_analysis.html` ⭐
- 통계 대시보드: `/dashboard.html`

### API
- 헬스 체크: `/health`
- API 헬스: `/api/health`
- 디버그: `/debug/info`

---

## 🔬 기술 스택

### 백엔드
- **FastAPI**: 고성능 웹 프레임워크
- **Python 3.9**: Rocky Linux 8.10
- **Uvicorn**: ASGI 서버

### AI/ML (구현 예정)
- **CLIP**: 이미지 임베딩 (openai/clip-vit-large-patch14)
- **LangChain**: RAG 파이프라인 프레임워크
- **FAISS**: 벡터 유사도 검색
- **LLaVA**: 멀티모달 LLM (llava-hf/llava-v1.6-mistral-7b-hf)
- **ko-sbert-nli**: 한국어 임베딩 (jhgan/ko-sbert-nli)

### 프론트엔드
- **HTML5/CSS3/JavaScript**: Vanilla JS (프레임워크 없음)
- **LocalStorage**: 브라우저 기반 데이터 저장

### 인프라
- **NCP**: Naver Cloud Platform
- **GPU 서버**: dm-gpu-vm (10.200.3.0/24)
- **ALB**: dm-alb-112319279
- **NLB**: dm-nlb-112319415

---

## 📄 업로드된 매뉴얼 정보

**파일**: defect_manual.pdf (4 페이지)

**포함 불량 유형** (9가지):
1. **Blowholes** (기공) - 탈기 불량으로 인한 내부 공극
2. **Cold Shot** (냉간 결합) - 용탕 접합 실패
3. **Fins** (날개 버) - 금형 틈새로 용탕 유출
4. **Flash** (플래시) - 금형 맞물림 불량
5. **Misrun** (주입 불충분) - 용탕 미도달
6. **Scar** (표면 스카) - 표면 냉각 불균일
7. **Shrinkage** (수축공) - 응고 중 체적 감소
8. **Crack** (균열) - 열응력으로 인한 파단
9. **Scratch** (긁힘) - 탈형 시 마찰

각 불량 유형마다:
- 발생 원인
- 조치 가이드
- 대표 위치
- 현장 조치 방법

---

## 🔍 트러블슈팅

### 1. 헬스 체크 실패
```bash
curl http://localhost:8080/health
curl http://localhost:8080/debug/info
```

### 2. 서비스 재시작
```bash
sudo systemctl restart similar-image.service
sudo journalctl -u similar-image.service -f
```

### 3. 로그 확인
```bash
tail -f /root/similar_image_matching/app.log
```

### 4. 파일 권한 문제
```bash
chmod 644 /root/similar_image_matching/*.html
chmod 755 /root/similar_image_matching/uploads
```

---

## 📝 향후 개선 계획

### Phase 1: 백엔드 통합 (1-2주)
- [ ] 실제 CLIP 모델 통합
- [ ] LangChain RAG 파이프라인 구현
- [ ] LLaVA 모델 통합
- [ ] PDF 벡터 DB 사전 구축

### Phase 2: 기능 확장 (1-2개월)
- [ ] Object Storage 연동
- [ ] 데이터베이스 연동 (PostgreSQL)
- [ ] 사용자 인증 시스템
- [ ] 실시간 처리 최적화

### Phase 3: 고도화 (3개월+)
- [ ] YOLO 세그멘테이션 추가
- [ ] 자동 학습 파이프라인
- [ ] 생산 라인 통합
- [ ] 모바일 앱 연동

---

## 💡 사용 예시

### 불량 분석 전체 프로세스

1. **데이터셋 준비** (`/manual_mapping.html`)
   - 정상 이미지 데이터셋 ZIP 파일 업로드
   - defect_manual.pdf 매뉴얼 업로드
   - 매핑 정보 입력 및 저장

2. **불량 이미지 분석** (`/defect_analysis.html`)
   - 불량 의심 이미지 업로드
   - "불량 분석 시작" 클릭
   - 4단계 자동 처리 관찰
   - 터미널 로그로 처리 과정 확인
   - AI 분석 결과 및 대응 방안 확인

3. **통계 확인** (`/dashboard.html`)
   - 매칭 성공/실패 통계
   - 불량 유형별 분포
   - CSV 데이터 내보내기

---

## 📞 문의 및 지원

- **버전**: 2.0
- **최종 업데이트**: 2025-11-03
- **작성자**: Claude AI Assistant

시스템 문제 발생 시:
1. `tail -f app.log` - 서버 로그
2. `systemctl status similar-image.service` - 서비스 상태
3. `curl http://localhost:8080/debug/info` - 디버그 정보
